Tweetify 
a Twitter clone built with HTML, CSS, JavaScript and React.
made this to learn how a real social media UI actually comes together — the feed, the composer, profiles, responsive layout, all of it. no backend, no shortcuts, just frontend fundamentals.

stack — HTML · CSS · JavaScript · React
run it — npm install then npm start

made this to learn. still learning.
